{
    "post": {
        "tags": [ "Photos" ],
        "security": [ { "ApiKeyAuth": [] } ],
        "summary": "Upload temp photo to the server",
        "parameters": [
            {
                "in": "path",
                "name": "id",
                "description": "Photo identifier",
                "schema": { "type": "string", "format": "uuid" },
                "required": true
            }
        ],
        "responses": {
            200: {
                "description": "OK"
            }
        }
    }
}
